TOUR IMAGES

Tour cards now render the Image path / URL field.
Use a direct image URL (ending in .jpg, .jpeg, .png, .webp when possible),
not a normal Pexels photo webpage URL.
Example of a page URL that will NOT work as an <img> source:
https://www.pexels.com/photo/...
A direct image/CDN URL is required.

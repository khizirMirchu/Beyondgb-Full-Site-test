STEP 6 PUBLIC TOURS FIX

This version connects pages/tours.html directly to GET /api/tours.
The ZIP intentionally does NOT include beyondgb.db.
After extracting, copy your current backend/beyondgb.db into backend/.
Do not run create-admin when using your existing database.
